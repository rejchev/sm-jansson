# vim: sts=2 ts=8 sw=2 tw=99 et ft=python:
import os

builder.SetBuildFolder('libjansson')

rvalue = {}
for cxx in builder.targets:
  binary = Extension.StaticLibrary(builder, cxx, 'jansson')
  
  jansson_root = os.path.join(builder.sourcePath)

  binary.compiler.includes += [
    os.path.join(jansson_root, 'include'),
    os.path.join(jansson_root, 'private_include'),
    os.path.join(jansson_root, 'jansson', 'src'),
  ]

  binary.compiler.defines += ['HAVE_CONFIG_H']

  if binary.compiler.family == 'clang':
    # https://llvm.org/bugs/show_bug.cgi?id=16428
    binary.compiler.cflags += ['-Wno-attributes']

  jansson_src = os.path.join(jansson_root, 'jansson', 'src')
  binary.sources += [
    os.path.join(jansson_root, 'dump.c'),
    os.path.join(jansson_root, 'error.c'),
    os.path.join(jansson_root, 'hashtable.c'),
    os.path.join(jansson_root, 'hashtable_seed.c'),
    os.path.join(jansson_root, 'load.c'),
    os.path.join(jansson_root, 'memory.c'),
    os.path.join(jansson_root, 'pack_unpack.c'),
    os.path.join(jansson_root, 'strbuffer.c'),
    os.path.join(jansson_root, 'strconv.c'),
    os.path.join(jansson_root, 'utf.c'),
    os.path.join(jansson_root, 'value.c'),
  ]

  rvalue[binary.compiler.target.arch] = builder.Add(binary)